// url_get.ncoda — capability header (provider = file name)
// desc: 提取链接
@language nodecoda/1

type UrlGetGetUrlResult = {
  string url;
};

@capability failure_branch=true, timeout_ms=180000
function get_url(any content) -> UrlGetGetUrlResult {}
